# Local compose only — NO VPS deploy, no host/SSH targets.
# Usage (from this directory):
#   docker compose up --build
# Optional live ingest (public WS only):
#   ENABLE_LIVE_INGEST=1 docker compose up --build
#   # or subset: LIVE_TIMEFRAMES=1m,5m,15m ENABLE_LIVE_INGEST=1 docker compose up --build
# Then: curl http://127.0.0.1:8080/health
services:
  market-data:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      ENABLE_LIVE_INGEST: "${ENABLE_LIVE_INGEST:-0}"
      LIVE_TIMEFRAMES: "${LIVE_TIMEFRAMES:-1m,5m,15m,1h,4h,1d,1w}"
    restart: "no"
