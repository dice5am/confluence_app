package com.cavin.confluence

import android.app.Application

/**
 * Confluence App — personal BTC/USDT charts + confluence insight alerts.
 * Insight only; never executes trades. No exchange keys on device (Arch-B).
 */
class ConfluenceApp : Application()
